<Page x:Class="SimVillage.PersistenceView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:SimVillage"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="Persistence" Background="LightGreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Button Content="Slot 1" Grid.Column="0" Command="{Binding Slot1Command}" HorizontalAlignment="Center" Margin="0,20,0,0" VerticalAlignment="Top" Height="60" Width="400"/>
        <Button Content="Slot 2" Grid.Column="0" Command="{Binding Slot2Command}" HorizontalAlignment="Center" Margin="0,100,0,0" VerticalAlignment="Top" Height="60" Width="400"/>
        <Button Content="Slot 3" Grid.Column="0" Command="{Binding Slot3Command}" HorizontalAlignment="Center" Margin="0,180,0,0" VerticalAlignment="Top" Height="60" Width="400"/>
        <Button Content="Slot 4" Grid.Column="0" Command="{Binding Slot4Command}" HorizontalAlignment="Center" Margin="0,260,0,0" VerticalAlignment="Top" Height="60" Width="400"/>
        <Button Content="Slot 5" Grid.Column="0" Command="{Binding Slot5Command}" HorizontalAlignment="Center" Margin="0,340,0,0" VerticalAlignment="Top" Height="60" Width="400"/>
        <Button Cursor="Hand" Width="120" Height="60" Command="{Binding ContinueGameCommand}" Margin="340,466,340,74">
            <Button.Content>
                <TextBlock Text="Back"/>
            </Button.Content>
        </Button>
        <Button Content="Delete" Grid.Column="1" Command="{Binding Slot1DeleteCommand}" HorizontalAlignment="Left" Margin="0,20,0,0" VerticalAlignment="Top" Height="60" Width="70"/>
        <Button Content="Delete" Grid.Column="1" Command="{Binding Slot2DeleteCommand}" HorizontalAlignment="Left" Margin="0,100,0,0" VerticalAlignment="Top" Height="60" Width="70"/>
        <Button Content="Delete" Grid.Column="1" Command="{Binding Slot3DeleteCommand}" HorizontalAlignment="Left" Margin="0,180,0,0" VerticalAlignment="Top" Height="60" Width="70"/>
        <Button Content="Delete" Grid.Column="1" Command="{Binding Slot4DeleteCommand}" HorizontalAlignment="Left" Margin="0,260,0,0" VerticalAlignment="Top" Height="60" Width="70"/>
        <Button Content="Delete" Grid.Column="1" Command="{Binding Slot5DeleteCommand}" HorizontalAlignment="Left" Margin="0,340,0,0" VerticalAlignment="Top" Height="60" Width="70"/>
    </Grid>
</Page>
